const arr = [1, 1, 6, 2, 8, 5, 7].sort((a, b) => a < b ? -1 : 1)
//const arr = [1, 1, 2, 4, 5, 7]
// const arr = [-1, -2, -3]
// const arr = [1, 2, 3]
const findMin = (a) => {

    let result = 1
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] === result)
            result += 1
        else if (arr[i] > result)
            return result
    }
    return result
}

console.log(findMin(arr))